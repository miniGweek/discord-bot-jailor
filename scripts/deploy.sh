#!/bin/bash

# This is used by github actions
# do not use for deployment from local. Use deploy.ps1 for local deployment
# scp main.py update_deployment.sh discord-bot-jailor.conf $1
 
# ssh $1 'chmod +x /staging/update_deployment.sh && /staging/update_deployment.sh'
